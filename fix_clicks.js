const fs = require('fs');
const path = require('path');
const glob = require('glob'); // Note: we might just use fs.readdirSync if glob is not installed, but let's assume standard node. 
// Or I can just write a shell loop.
